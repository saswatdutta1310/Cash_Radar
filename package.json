{
  "name": "cash_radar",
  "version": "1.0.0",
  "description": "SME Cash Flow Intelligence Engine",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "start": "concurrently \"npm run dev --workspace=apps/web\" \"npm start --workspace=apps/api\" \"cd apps/simulation && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --reload\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
